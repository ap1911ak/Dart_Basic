// ข้อ 11: ฟังก์ชันบวกเลข
// โจทย์: สร้างฟังก์ชันชื่อ add ที่รับพารามิเตอร์เป็นตัวเลขจำนวนเต็ม 2 ตัว และคืนค่า (return) เป็นผลรวมของเลขทั้งสอง จากนั้นเรียกใช้ฟังก์ชันนี้และแสดงผลลัพธ์

// ข้อจำกัด: ต้องสร้างฟังก์ชันแยกต่างหาก ไม่ใช่เขียนโค้ดใน main() อย่างเดียว

void main() {
  print(add(50,60));
}

add(int a,b)=>a+b;

//Run on DartPad
